{
  "name": "@raaghas/database",
  "version": "1.0.0",
  "private": true,
  "main": "./generated-client/index.js",
  "types": "./generated-client/index.d.ts",
  "scripts": {
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "generate": "prisma generate || echo 'Local bypass'",
    "build": "[ -d generated-client ] && echo '[@raaghas/database] generated-client exists, skipping generate.' || ../../node_modules/.bin/prisma generate --schema=prisma/schema.prisma",
    "start:prod": "echo '[@raaghas/database] This is a library package — no server to start. OK.'"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^6.7.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@prisma/client": "^6.7.0",
    "@types/pg": "^8.11.10",
    "prisma": "^6.7.0"
  }
}
